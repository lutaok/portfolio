import { Component, JSX } from "solid-js";
import { TimelineStepProps } from "./Timeline";

const TimelineStep: Component<TimelineStepProps> = (props) => {
  return props as unknown as JSX.Element;
};

export default TimelineStep;
